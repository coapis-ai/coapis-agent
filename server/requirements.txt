# ═══════════════════════════════════════════════════════════════════
# CoApis-agent 生产环境依赖
# Python 版本要求: >=3.10,<3.14 (推荐 3.11)
# 生成时间: 2026-07-01
# 基于源码扫描 + QwenPaw 对比分析
# ═══════════════════════════════════════════════════════════════════

# ── 核心框架（P0 补充） ──
agentscope==1.0.19.post1
agentscope-runtime==1.1.4
fastapi>=0.100.0,<1.0.0        # P0: 源码中 100+ 文件使用
pydantic>=2.0.0,<3.0.0          # P0: 源码中 64+ 文件使用
pydantic-settings>=2.0.0
uvicorn>=0.40.0
starlette>=0.27.0
click>=8.0.0                    # P0: CLI 框架

# ── HTTP/通信（P0 补充） ──
httpx>=0.27.0
aiohttp>=3.9.0                  # P0: 渠道/工具使用
anyio>=4.0.0,<4.13.0           # 注意: 4.13.0 有 busy-loop bug
aiofiles>=24.1.0
websockets>=13.0                # P0: WebSocket 支持

# ── 数据/加密 ──
pyyaml>=6.0
json-repair>=0.30.0
orjson>=3.9                     # P1: 高性能 JSON，QwenPaw 有
packaging>=24.0
cryptography>=43.0.0
bcrypt>=4.0.0

# ── CLI/交互（P1 补充） ──
questionary>=2.1.1
rich>=13.0.0                    # P1: 终端美化，QwenPaw 有

# ── 定时任务 ──
apscheduler>=3.11.2,<4

# ── 系统工具（P1 补充） ──
python-dotenv>=1.0.0
shortuuid>=1.0.0
keyring>=25.0.0
mss>=9.0.0
pillow>=10.0.0
psutil>=6.0.0                   # P1: 系统监控，QwenPaw 有
frontmatter>=2.0.0              # P1: YAML 前处理，QwenPaw 有

# ── 协议/通信（P0 补充） ──
paho-mqtt>=2.0.0                # MQTT 客户端
agent-client-protocol>=0.9.0    # ACP 协议
mcp>=1.0.0                      # P0: MCP 协议，源码中使用

# ── 中文处理 ──
jieba>=0.42.0
segno>=1.6.6

# ── 企业微信 ──
wecom-aibot-python-sdk==1.0.2

# ═══════════════════════════════════════════════════════════════════
# 渠道依赖（按需安装）
# ═══════════════════════════════════════════════════════════════════

# ── Discord ──
discord-py>=2.3

# ── 钉钉（P0 补充 alibabacloud-tea-util） ──
dingtalk-stream>=0.24.3
alibabacloud-dingtalk>=2.2.42
alibabacloud-tea-openapi>=0.4.4
alibabacloud-tea-util>=0.3.0    # P0: 阿里云 SDK 工具类，QwenPaw 有

# ── 飞书 ──
lark-oapi>=1.5.3

# ── Telegram ──
python-telegram-bot>=20.0

# ── SMS/语音 ──
twilio>=9.10.2

# ── Matrix ──
matrix-nio>=0.24.0

# ── 桌面窗口 ──
pywebview>=4.0

# ═══════════════════════════════════════════════════════════════════
# AI 提供商依赖（P0 补充 anthropic/openai）
# ═══════════════════════════════════════════════════════════════════

# ── Claude（P0 补充） ──
anthropic>=0.10.0

# ── OpenAI（P1 补充，QwenPaw 有） ──
openai>=2.0.0,<=2.33.0

# ── Gemini ──
google-genai>=1.67.0

# ═══════════════════════════════════════════════════════════════════
# 可选依赖（P2，按需启用）
# ═══════════════════════════════════════════════════════════════════

# ── 浏览器自动化 ──
# playwright>=1.49.0

# ── 本地模型 ──
# huggingface_hub>=0.20.0
# transformers>=4.30.0
# modelscope>=1.35.0
# onnxruntime<1.24

# ── 记忆系统 ──
# reme-ai==0.3.1.8

# ── 文件热重载 ──
# watchfiles>=0.22

# ── Coding Mode ──
# python-lsp-server[all]>=1.10
# ast-grep-cli>=0.20

# ── Python 3.13 兼容 ──
# audioop-lts; python_version >= '3.13'
