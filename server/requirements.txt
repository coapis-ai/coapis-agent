# ═══════════════════════════════════════════════════════════════════
# CoApis-agent 生产环境依赖
# Python 版本要求: >=3.10,<3.14 (推荐 3.11)
# 生成时间: 2026-07-02
# 与 pyproject.toml 同步维护 — 单文件即可安装
# ═══════════════════════════════════════════════════════════════════

# 推荐安装方式（二选一）:
#   pip install -e .                                    # 从 pyproject.toml 安装
#   pip install -r requirements.txt                     # 从本文件安装
#
# 如需企业微信渠道:
#   pip install -e ".[wecom]"                           # 从 pyproject.toml 安装
#   pip install -r requirements.txt                     # 安装基础
#   pip install wecom-aibot-python-sdk                  # 单独安装企业微信 SDK
#
# 如需浏览器自动化:
#   pip install -e ".[browser]"
#   playwright install chromium

# ── 核心框架 ──
agentscope>=1.0.0,<2.0.0
agentscope-runtime>=1.1.0,<2.0.0
fastapi>=0.100.0,<1.0.0
pydantic>=2.0.0,<3.0.0
pydantic-settings>=2.0.0
uvicorn>=0.40.0
starlette>=0.27.0
click>=8.0.0

# ── HTTP/通信 ──
httpx>=0.27.0
aiohttp>=3.9.0
aiofiles>=24.1.0
websockets>=13.0
anyio>=4.0.0

# ── 数据/加密 ──
pyyaml>=6.0
json-repair>=0.30.0
orjson>=3.9
packaging>=24.0
cryptography>=43.0.0
bcrypt>=4.0.0

# ── CLI/交互 ──
questionary>=2.1.1
rich>=13.0.0

# ── 定时任务 ──
apscheduler>=3.11.2,<4

# ── 系统工具 ──
python-dotenv>=1.0.0
shortuuid>=1.0.0
keyring>=25.0.0
mss>=9.0.0
pillow>=10.0.0
psutil>=6.0.0
python-frontmatter>=1.3.0

# ── 协议/通信 ──
paho-mqtt>=2.0.0
agent-client-protocol>=0.9.0
mcp>=1.0.0

# ── 中文处理 ──
jieba>=0.42.0
segno>=1.6.6

# ── 渠道（核心） ──
discord-py>=2.3
dingtalk-stream>=0.24.3
alibabacloud-dingtalk>=2.2.42
alibabacloud-tea-openapi>=0.4.4
alibabacloud-tea-util>=0.3.0
lark-oapi>=1.5.3
python-telegram-bot>=20.0
twilio>=9.10.2
matrix-nio>=0.24.0
pywebview>=4.0

# ── AI 提供商 ──
google-genai>=1.67.0
openai>=2.0.0
anthropic>=0.10.0

# ── 其他 ──
python-socks>=2.5.3
tzdata>=2024.1

# ═══════════════════════════════════════════════════════════════════
# 可选依赖（按需安装）
# ═══════════════════════════════════════════════════════════════════

# ── 企业微信（私有包，不在公共 PyPI） ──
# wecom-aibot-python-sdk>=1.0.0

# ── 浏览器自动化 ──
# playwright>=1.49.0

# ── 本地模型 ──
# huggingface_hub>=0.20.0

# ── 语音识别 ──
# openai-whisper>=20231117

# ── SIP/VoIP ──
# pyVoIP>=1.6.0
# dashscope>=1.0.0
# dashscope-realtime>=0.1.0
