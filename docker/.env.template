# 复制为 .env 并填写实际值
COAPIS_WORKING_DIR=/apps/ai/coapis
COAPIS_WEB_PORT=4200
COAPIS_SERVER_PORT=4208
COAPIS_AUTH_ENABLED=true
COAPIS_LOG_LEVEL=INFO

# ── 数据库（可选，决策 D-8/D-9）──
# 社区版仅 SQLite；相对路径相对 COAPIS_WORKING_DIR 解析，
# 且文件必须落在挂载卷内（容器重建不丢）。
# 默认（不设置）= <COAPIS_WORKING_DIR>/system/coapis.db，已满足 D-9。
# COAPIS_DATABASE_URL=system/coapis.db
