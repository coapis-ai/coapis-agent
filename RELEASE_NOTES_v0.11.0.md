# v0.11.0 - 外部登录修复与场景管理优化

发布日期：2026-07-24

## 核心修复

### 🔧 外部系统自动登录修复
- **问题**：外部系统自动登录后无法聊天
- **根因**：用户智能体模型配置缺少`model_type`字段
- **解决**：删除用户配置中的`model`、`provider`、`active_model`字段，使用默认模型配置

### 🔧 外部用户登录数据修复
- **问题**：外部用户登录数据缺少`id`字段
- **根因**：`user_store.py`的`create_user()`函数没有自动生成id
- **解决**：增加id自动生成逻辑

### 🔧 场景智能体叠加修复
- **问题**：场景智能体叠加失败，场景专业性不明显
- **根因**：部分场景AGENTS.md内容过于简单（< 200字符）
- **解决**：批量优化4个简单场景的AGENTS.md

### 🔧 上下文压缩持久化修复
- **问题**：上下文压缩成功但下次发消息又恢复到压缩前状态
- **根因**：`AgentContext`类缺少`update_compressed_summary()`方法
- **解决**：添加`update_compressed_summary()`方法和初始化`_compressed_summary`属性

### 🔧 嵌入浮窗聊天窗口修复
- **问题**：嵌入浮窗聊天窗口无法选择文字
- **根因**：全局`user-select: none`样式
- **解决**：精细化控制，只在拖拽时禁用

### 🔧 登录后自动跳转修复
- **问题**：登录后自动跳转到home路径
- **根因**：路由匹配优先级问题
- **解决**：调整路由顺序，`/`放在`/login`之前

### 🔧 工作场景二级菜单修复
- **问题**：点击二级菜单后显示空白页面
- **根因**：路由参数获取错误（query vs path parameter）
- **解决**：使用`useParams`获取路径参数

### 🔧 工作台API路由修复
- **问题**：`/api/scenes/recommendations`返回404
- **根因**：FastAPI路由顺序问题
- **解决**：固定路径在前，参数化路由在后

## 新功能

### ✨ 动态菜单系统
- 二级菜单动态加载（从标签管理系统）
- 菜单配置集中管理
- 支持权限过滤

### ✨ 场景管理优化
- 首页6个卡片组件对接真实API
- 工作台场景列表展示
- 二级菜单实现（12个业务领域）

### ✨ 标签系统优化
- 三层分类体系（dimension → category → scene）
- 交叉标签支持（industry标签）
- 频率标签（高频、中频、低频）

### ✨ 空分类场景补充
- 为6个空分类创建18个基础场景
- 每个分类至少2个场景
- 总场景数：47个

### ✨ 镜像命名规范
- 统一为`coapis-agent:latest`
- Docker Compose文件配置统一

## 技术改进

### 🏗️ 智能体ID双字段设计
```json
{
  "id": "user:张三",        // 语义ID（可含中文）
  "agent_id": "agent:20"    // ASCII-safe内部ID（用于HTTP headers）
}
```

### 🏗️ 场景智能体架构
- 场景系统提示词存储在`AGENTS.md`
- 运行时叠加：用户智能体 + 场景智能体
- 提示词前置注入

### 🏗️ 外部登录API
- HMAC-SHA256签名验证
- 自动创建用户和工作空间
- 支持中文用户名

### 🏗️ 用户工作空间初始化
- 统一初始化流程
- 自动创建agent.json
- 支持语言配置

## 升级说明

### Docker镜像更新
```bash
# 拉取新镜像
docker pull coapis-agent:v0.11.0

# 重新部署
cd docker
docker compose -f docker-compose.yml up -d --force-recreate server
```

### 配置变更
- SSO密钥：`COAPIS_SSO_SECRET`（生产环境必须设置）
- 镜像名称：统一使用`coapis-agent:latest`

## 已知问题
- 工作空间初始化日志中可能显示警告，但不影响功能
- 部分场景AGENTS.md可能需要进一步优化

## 贡献者
- 蜜总裁 🐝💼
